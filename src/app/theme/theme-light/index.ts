import { lightColors } from "./colors";
import { lightTypography } from "./typography";
import { lightScroll } from "./scroll";

export const lightTheme = {
  mode: "light",
  colors: lightColors,
  typography: lightTypography,
  scroll: lightScroll,
};
